name: 🔗 GHA
on:
  push:
    paths-ignore:
      - '**/README.md'
      - '**/CONTRIBUTING'
      - '**/LICENSE'

jobs:
  linux-build:
    name: 🐧 Linux
    uses: ./.github/workflows/linux-builds.yml
