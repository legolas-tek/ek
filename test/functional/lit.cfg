import lit.formats

config.name = 'GLaDOS Functional Tests'
config.test_format = lit.formats.ShTest(execute_external=True)
config.suffixes = ['.ek']

config.substitutions = [
    ('glados', '../../glados'),
    ('ekrun', 'cd ../../stdlib; ../ekc -t result')
    # ('glados', 'scheme -q'),
]
    
