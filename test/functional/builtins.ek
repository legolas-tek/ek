//
// RUN: echo "test" | (ekrun %s) 2>%t | FileCheck -v %s && FileCheck --check-prefix ERROR -v %s < %t
//

import std

// CHECK: 42
// CHECK: test
// ERROR: hello
fn main = print (42 toString) >> print readLine >> eprint "hello"
